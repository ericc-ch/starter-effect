# Alchemy configuration
# - ALCHEMY_STATE_TOKEN: Required for multi-machine deployments (generate with: openssl rand -base64 32)
#   Same token shared across all environments in one Cloudflare account
# - ALCHEMY_PASSWORD: Encrypts secrets in state files (generate with: openssl rand -base64 32)
#   WARNING: If lost, encrypted secrets become unrecoverable - use different passwords for dev vs main
# - ALCHEMY_STAGE: Deployment environment ("dev" or "main")
# - ALCHEMY_REMOTE_STATE: Use CloudflareStateStore instead of local FileSystemStateStore ("true" or "false")
#   Set to "true" for CI/CD or when managing remote environments locally
ALCHEMY_STATE_TOKEN=
ALCHEMY_PASSWORD=
ALCHEMY_STAGE=dev
ALCHEMY_REMOTE_STATE=false

# Custom domains (required when ALCHEMY_REMOTE_STATE=true)
# These bind your Cloudflare Workers to your custom domain
WEB_DOMAIN=
API_DOMAIN=
